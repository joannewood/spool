pytest==8.*
psycopg[binary]==3.2.*

# services/api's runtime deps, needed on the host so `spool_api` (and its
# FastAPI TestClient-based route tests) can actually import — pinned to
# match services/api/requirements.txt. uvicorn itself isn't needed here;
# TestClient talks to the FastAPI app object directly over an in-process
# ASGI transport, no real server involved.
fastapi==0.115.*
jinja2==3.1.*
python-multipart==0.0.*
httpx==0.27.*
