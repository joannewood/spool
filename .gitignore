# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Env / secrets
.env
.env.*
!.env.example

# Docker
docker-compose.override.yml

# Generated at runtime
thumbnails/
*.log
.claude/skills/run-spool/screenshots/

# Docker auto-creates these as empty dirs when LIBRARY_HOST_PATH/
# DOWNLOADS_HOST_PATH are left blank in .env (see docker-compose.yml's
# bind-mount fallbacks) — never anything worth committing.
/data/unused-library/
/data/unused-downloads/

# macOS
.DS_Store

# Claude Code local settings
.claude/settings.local.json

# Internal build log / working notes for Claude Code — a personal,
# decision-by-decision record kept for continuity across sessions, not
# meant for a shared/public clone. Anything in here relevant to actually
# running SPOOL has been folded into README.md instead.
CLAUDE.md

# Editors
.vscode/
.idea/
